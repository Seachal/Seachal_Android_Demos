<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 选中状态 -->
    <item android:state_checked="true">
        <shape android:shape="oval">
            <solid android:color="@color/radiobutton_red_checked" />
            <stroke
                android:width="2dp"
                android:color="@color/radiobutton_red_checked" />
            <size
                android:width="20dp"
                android:height="20dp" />
        </shape>
    </item>
    
    <!-- 未选中状态 -->
    <item android:state_checked="false">
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="2dp"
                android:color="@color/radiobutton_red_normal" />
            <size
                android:width="20dp"
                android:height="20dp" />
        </shape>
    </item>
    
    <!-- 默认状态 -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="2dp"
                android:color="@color/radiobutton_red_normal" />
            <size
                android:width="20dp"
                android:height="20dp" />
        </shape>
    </item>
</selector> 