<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- 按下状态 -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="8dp" />
            <solid android:color="@color/button_secondary_pressed" />
            <stroke android:width="1dp" android:color="@color/button_border" />
        </shape>
    </item>
    
    <!-- 禁用状态 -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <corners android:radius="8dp" />
            <solid android:color="@color/progress_background_color" />
            <stroke android:width="1dp" android:color="@color/progress_background_color" />
        </shape>
    </item>
    
    <!-- 默认状态 -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="8dp" />
            <solid android:color="@color/button_secondary" />
            <stroke android:width="1dp" android:color="@color/button_border" />
        </shape>
    </item>
    
</selector> 